name: Run Python Script

on:
  push:
    branches:
      - main

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: latest  # Replace 'x' with the desired Python version

    - name: Run Python Script
      run: |
        python Git Log.py  # Replace 'your_script.py' with the actual filename
